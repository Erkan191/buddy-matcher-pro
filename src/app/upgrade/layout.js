import { pageMetadata } from "@/lib/seo";

export const metadata = pageMetadata({
  title: "Pro Group Generator — £3.99 One-Off",
  description: "Get Buddy Matcher Pro for £3.99 once. Unlock larger groups, pairing rules, saved lists, Student View, CSV export and clean printing. No subscription.",
  path: "/upgrade",
});

export default function UpgradeLayout({ children }) { return children; }
